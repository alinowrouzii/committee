"use strict";
var express = require('express');
var get_token = require('./../controllers/tokenController').get_token;
var router = express.Router();
// router.post('/token', createPost);
module.exports = router;
